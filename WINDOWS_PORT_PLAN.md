# Windows移植 実装計画

`WINDOWS_HANDOFF.md` を受けて、Windows実機（2026-07-27時点）でコードと環境を確認した結果と、移植の実装計画。

結論: **引き継ぎメモの前提「32GB RAM, CPU-only」が実機とズレており、7項目のうち2つは「確認・調整」ではなく設計変更が必要**。加えてメモに無い優先度の高い論点が3つある。

## 1. 実機環境（実測）

| 項目 | 状態 |
|---|---|
| Node.js | 22.15.0 ✅ |
| VS Build Tools 2019 (VC++ x64) + Windows SDK 10.0.19041 | インストール済み ✅ |
| WebView2 Runtime | 150.0.4078.99 ✅ |
| ディスク空き (C:) | 454 GB ✅ |
| Rust / cargo | 1.97.1 / stable-x86_64-pc-windows-msvc **導入済み**。ただし `~/.cargo/bin` が User PATH に無い ⚠️ |
| Ollama | クライアント 0.23.2、**未起動・モデル0件** |
| CPU | **Core i7-1185G7 — 4コア/8スレッド、15〜28W のモバイル (Tiger Lake)** |
| RAM | 31.7 GB **LPDDR4X-4267 / 128bit → 理論帯域 68.3 GB/s** |
| GPU | Intel Iris Xe（Ollama公式Windowsビルドは CUDA/ROCm のみ対応 → **真のCPU-only**） |

メモが想定していそうな「32GBのデスクトップCPU」ではなく **4コアの薄型ノート**。ここが以降の評価を大きく変える。

## 2. CPU-onlyでの速度（実測）

トークン生成はメモリ帯域律速（`tok/s ≒ 実効スループット ÷ アクティブサイズ`）。実測で得た**このマシンの重み読み出しスループットは 49.6 GB/s**。

この数字の出し方には試行錯誤があり、記録しておく価値がある。当初は合成メモリベンチマークを書いたが、**最適化レベルで結果が激変して使えなかった**:

| 測定方法 | debug (opt-level 0) | release (opt-level 3) |
|---|---|---|
| 手書き加算ループ | 3.4 GB/s | 45.4 GB/s |
| memcpy (`copy_from_slice`) | 26.3 GB/s | 80.4 GB/s（理論値68.3超＝そもそも不正） |

`tauri dev` と `tauri build` で推奨モデルが変わるアプリは、「まだ分からない」と言うアプリより悪い。そこで**合成ベンチマークを捨て、実際の生成から測る**方式に変更した。Ollamaは毎レスポンスで `eval_count` / `eval_duration` を返すので、`モデルサイズ × 実測tok/s` が求める値そのものになる。この方式はビルドプロファイルに依存せず（Ollama自身がreleaseバイナリ）、**debugビルドのテストから 49.6 GB/s** を得た — releaseでのスタンドアロン実測 45.4 GB/s とよく一致する。GPUオフロードの有無も自動的に織り込まれる。

### 実モデルによる検証と、キャリブレーションの修正

`llama3.2:3b` / `gemma3:4b` / `llama3.1:8b` を実際に入れて予測と突き合わせたところ、**推定が系統的に楽観側へずれていた**。

| モデル | サイズ | 予測(49.6 GB/s基準) | 実測 | 誤差 | 実測から逆算した帯域 |
|---|---|---|---|---|---|
| llama3.2:3b | 2.0 GB | 24.8 | **18.2 tok/s** | −27% | 36.4 GB/s |
| gemma3:4b | 3.3 GB | 15.0 | **14.3 tok/s** | −5% | 47.2 GB/s |
| llama3.1:8b | 4.9 GB | 10.1 | **7.7 tok/s** | −24% | 37.7 GB/s |

原因は**キャリブレーションの取得元が 270MB の `smollm2:135m` だったこと**。小さいモデルは重みがキャッシュに乗りやすく、かつファイルサイズに対して語彙・埋め込みの比率が高い（これらは毎トークン全部読まれない）ため、ファイルサイズが1トークンあたりの実読み出し量を大きく過大評価する。速度ゲートにとって**楽観側の誤りは有害**（遅いモデルを「使える」と推奨してしまう）。

**修正**: キャリブレーションのサンプルに**1.0GB のサイズ下限**を設けた。これで 270MB や 522MB のモデルは測定に寄与しなくなる。実機で `llama3.2:3b` から採り直すと **30.3 GB/s**（ベンチ値36.4より低いのは、こちらがウォームアップなしの短い生成だから）。低く出るのは安全側なので許容する。指数平滑（重み0.3）で複数回の生成に応じて落ち着く。

修正後の推定と実測の一致:

| モデル | 修正後の予測 | 実測 | 判定 |
|---|---|---|---|
| llama3.2:3b | 18.2 tok/s | 18.2 | 一致 |
| gemma3:4b | 11.0 tok/s | 14.3 | 保守側（−23%） |
| llama3.1:8b | 7.4 tok/s | 7.7 | 保守側（−4%） |

以降すべて保守側に振れており、ゲートが過大な約束をしない。

### 現在の推奨リスト（実測キャリブレーション適用後）

```
llama3.2:3b     2.0G  active 2.0G  ~18.2 tok/s
gemma3:4b       3.3G  active 3.3G  ~11.0 tok/s
gpt-oss:20b    13.0G  active 3.6G  ~10.1 tok/s   ← MoE
llama3.1:8b     4.9G  active 4.9G   ~7.4 tok/s
deepseek-r1:8b  5.2G  active 5.2G   ~7.0 tok/s
qwen3.5:9b      6.6G  active 6.6G   ~5.5 tok/s
mistral-nemo    7.1G  active 7.1G   ~5.1 tok/s
除外: mistral-small:22b (2.8) / gemma3:12b (4.5) / phi4:14b (4.0)
```

同じ13GBでも、MoEの `gpt-oss:20b` は密モデルの `mistral-small:22b` の**3.6倍速い**。RAM可否は総サイズ、速度はアクティブサイズで別々に評価する必要がある。

### プレフィル（実測）

**プレフィルは演算律速**という主張も検証した。生成速度とは比例せず、生成の約3〜3.7倍で推移する。

| モデル | 生成 | プレフィル | 1000トークンのプロンプト |
|---|---|---|---|
| llama3.2:3b | 18.2 tok/s | **66.8 tok/s** | 約15秒 |
| gemma3:4b | 14.3 tok/s | **46.9 tok/s** | 約21秒 |
| llama3.1:8b | 7.7 tok/s | **23.4 tok/s** | **約43秒** |

当初「8B級で20〜40 tok/s、1000トークンで25〜50秒」と見積もったが、llama3.1:8b の実測は 23.4 tok/s / 43秒で見積り通りだった。**履歴上限（6往復）と短プロンプトのルータースキップという2つの制約は、この数字によって正当化される**。15Wクラスなので持続負荷ではサーマルスロットリングも入る。

## 3. 引き継ぎメモ7項目への所見

### ① keyringクレートの `windows-native`【検証完了 ✅】

Windows実機で完全に動作することを確認した。

- **ビルド**: `cargo check` 通過（1分36秒）。`keyring 3.6.3` が `windows-sys 0.59` でコンパイルされ、`security-framework` は**ビルドされない** → `apple-native` が keyring 側で正しく target-gate されている確証
- **保存・取得・上書き・削除・二重削除**: 実機の資格情報マネージャーに対してラウンドトリップ成功
- **プロセスを越えた永続化**: プロセスAで保存 → **別プロセス**Bで読み出し成功。in-processキャッシュではなくOSストアに到達している証明
- **OS側からの独立確認**: `cmdkey /list` に実在を確認。ターゲット名は
  `LegacyGeneric:target=api_key_<provider>.com.fjdev.localagentsops` / Type: Generic
- **後片付け**: 削除後に `cmdkey /list` から消えることも確認

`credentials.rs` に回帰テストを追加済み:
```
cargo test --lib credentials                                     # ラウンドトリップ
cargo test --lib -- --ignored persists_across_processes_write     # プロセス1で書く
cargo test --lib -- --ignored persists_across_processes_read      # プロセス2で読む
```

**実装漏れ（修正済み）**: `src/Settings.tsx` が「キーはこのMac上のKeychainに保存され」とハードコードされていた。バックエンドの `detect_hardware().os` を渡し、Windows では「Windowsの資格情報マネージャー」と表示するようにした。

### ② recommend() のCPU非考慮 → 実際は明確なバグ

`src-tauri/src/catalog.rs:85` が `size_gb` の**降順**ソートなので、ベンダー横断ラウンドロビンが「各社の一番大きいモデル」から選ぶ。本機（予算 31.7 × 0.7 = 22.3GB）での実際の出力順:

```
Round 0: qwen3.5:9b(6.6) / gemma3:12b(8.1) / mistral-small:22b(13.0) /
         llama3.1:8b(4.9) / deepseek-r1:8b(5.2) / phi4:14b(9.1) / gpt-oss:20b(13.0)
Round 1: gemma3:4b(3.3) / mistral-nemo(7.1) / llama3.2:3b(2.0)
```

上表で ✕ 判定の 13GB×2 と 9.1GB がリスト最上位に来て、唯一快適な `llama3.2:3b` / `gemma3:4b` が最下位。**CPU機では完全に逆順**。

**MoEの扱い**: `qwen3-coder:30b`（アクティブ3B）のようなMoEはアクティブパラメータ分の帯域コストで済むため**CPUでは速度的に理想的**。しかし総サイズ19GBに現行の一律1.6倍オーバーヘッドを掛けると予算を超えて弾かれる。MoEは重み常駐＋KVで1.2倍程度が妥当なので、**RAM可否は総サイズ、速度はアクティブサイズ**で分けて評価すべき（ただし本機32GBでは 19×1.2=22.8 でギリギリ超過し、実用は厳しい）。

### ③ Token Killer の親モデル呼び出し速度

まず注記: メモの「`router.rs` が `think: false` を明示」は実際には `src-tauri/src/engines/ollama.rs:88` 側の固定値で、`src-tauri/src/router.rs:103` の `true` は `force_json` 引数。効果としてはメモの通り。

本機では**9B級を親にするとプレフィルだけで20〜40秒**かかる。「体感が悪ければ軽量モデルに」ではなく、最初から `llama3.2:3b` / `gemma3:4b` 固定を前提にすべき。

さらに構造的な矛盾がある。`src-tauri/src/router.rs:89` の指示3が「短い質問はそのままコピーせよ」と言っているのに、**その判定のために毎回モデルを呼んでいる**。`deterministic_trim` と同じ発想で「履歴が短く新規メッセージも短い → ルーター呼び出し自体をスキップ」を前段に入れるべき。

### ④ パッケージング【検証完了 ✅】

`targets` を `["nsis"]` に絞ったうえで `npm run tauri build` を実走させ、**インストーラの生成に成功**した。

| 項目 | 結果 |
|---|---|
| release ビルド時間 | **3分57秒**（10〜20分の見積りは過大だった） |
| 実行ファイル | `target/release/local_agents_ops.exe` — 11.4MB |
| インストーラ | `target/release/bundle/nsis/LocalAgentsOps_0.1.0_x64-setup.exe` — **2.6MB** |
| 署名状態 | `NotSigned`（⑤のとおり想定内） |
| 警告 | 既存の未使用フィールド2件＋リンカの情報メッセージ1件のみ |

NSIS ツールチェインの自動ダウンロードもこの環境で問題なく通った。プロキシ・オフライン環境では失敗しうる点は変わらないので、別端末でビルドする場合は要注意。

MSI（WiX 3）は生成対象から外した。非ASCIIパスや制限された端末で失敗しやすく、社内配布に2つの形式を持つ利点よりコストが大きい。

### ⑤ コード署名

未署名 → NSIS配布ではSmartScreen警告は確実に出る。社内限定配布なら許容範囲か要判断。許容する場合は回避手順を社内向けに文書化する。

### ⑥ IME入力

WebView2 (Chromium) では `isComposing` が正しく立つので `src/App.tsx:480` の三重チェックは効く。

ただし**逆方向のリスク**がある。Windows IMEでは `compositionend` が `keydown` より先に発火するケースがあり、その瞬間は `isComposingRef=false` / `isComposing=false` / `keyCode=13` になって**三重チェックすべてをすり抜けて誤送信**し得る。macOS向けの対策ではこのパターンを救えない。

対策案: `compositionend` 直後の短時間（~100ms）のEnterを無視するガードを追加、または Ctrl+Enter 送信への変更（IMEユーザーには後者が安全）。

### ⑦ アイコン

問題なし。`Square*Logo.png` はMSIX用で、NSIS/MSI は `.ico` のみ使用。

## 4. メモに無い、優先度の高い論点

### (a) 並列比較の真の制約はRAMであって帯域ではない【当初の分析を実測で訂正】

当初「CPU-onlyでは同じコアと帯域を分割するだけなので各モデルが1/N速度になり、並列比較は機能として成立しない」と書いた。**実測したところこれは誤りだった。**

2つの異なるモデルを同時に走らせた計測（`smollm2:135m` と `qwen3:0.6b`、各120トークン上限）:

| | 単独 | 2モデル同時 |
|---|---|---|
| smollm2:135m | 167.0 tok/s | 95.4 tok/s（単独比 57%） |
| qwen3:0.6b | 63.0 tok/s | 50.6 tok/s（単独比 80%） |
| 同じ仕事の実時間 | 6.5秒（逐次） | **3.8秒（並列）** |

個々のモデルは遅くなるが、**実時間では並列のほうが速い**。eval時間ベースの合計スループットも 100 → 146 tok/s に増えている。単独の smollm2 がすでに 167 tok/s × 0.27GB ≒ 45 GB/s で帯域を飽和させており、2モデルでは合計約52 GB/s まで伸びる。帯域律速という物理は正しかったが、「並列は無意味」という結論は導けない。

したがって並列実行そのものを止める理由はなく、**制約すべきは同時に常駐させるRAM量**である。5〜13GBの実モデルを3つ載せれば予算22.2GBを超え、Ollamaが追い出しと再読み込みを繰り返す。

対応（実装済み）: 個数の固定上限ではなく**RAM予算に基づく同時実行制御**。各リクエストがモデルの想定常駐量に比例したセマフォ許可を取り、予算が埋まっている間は待つ。小さいモデルは並列に走り、大きいモデルだけが直列化される。待機中は `Queued` イベントでUIに「待機中（メモリ空き待ち）」と表示する。

加えて `src/App.tsx` の**起動時に全モデルを選択**（および pull したモデルの自動選択）は、どちらもこの端末では最初のターンを最も遅くする挙動なので変更した。

### (b) 「じっくり」モードが非thinkingモデルを巻き込んで失敗する【実測で確認・修正済み】

当初「`think: false` が非thinkingモデルでエラーになるのではないか」と推測したが、**実機で検証したところこれは誤り**だった。Ollama 0.23.2 に `smollm2:135m`（非thinking）で直接リクエストした結果:

| 送信内容 | 結果 |
|---|---|
| `think` フィールドなし | ✅ 正常 |
| `"think": false` | ✅ **正常**（懸念は外れ） |
| `"think": false` + `"format":"json"`（routerの組み合わせ） | ✅ 正常 |
| `"think": true` | ❌ **`{"error":"\"smollm2:135m\" does not support thinking"}`** |

**実際のバグは `think: true` 側**だった。「🧠 じっくり」は選択中の全モデルに一律で適用される（`sendToTarget` が全ターゲットに同じ `think` を渡す）ため、非thinkingモデルを1つでも含む並列比較では**そのモデルのターンが400で丸ごと失敗する**。しかも非thinkingなのは gemma3 / llama3.x / phi4 / mistral で、**本機で実用になるモデル群がすべて該当**する（thinking対応は qwen3.x と deepseek-r1 のみ）。macOS側はカタログが qwen3.x / deepseek-r1 寄りだったため踏まなかったと考えられる。Windows固有ではないが、この端末では確実に踏む。

**修正済み**: `/api/show` が `capabilities: ["completion", ...]` を返すので、これで判定して非対応なら `think` をペイロードから省き、エラーではなく通常応答に降格させる。`ChatRequest.think` を `Option<bool>` + `skip_serializing_if` に変更。`ModelInfo.supports_thinking` をUIまで通し、じっくりモード時に対象外モデルを明示するようにした。実機Ollamaに対する回帰テスト2件つき（`cargo test --lib -- --ignored engines::ollama`）。

なお `chat_once` の `"think": false` 固定は**意図的に維持**する。thinking対応モデルを親モデルにした場合、フィールドを省くとデフォルトで熟考に入ってルーターが遅くなるため、抑制指定が必要。

**追記（Ollama 0.32.4 更新後に再検証）**: 挙動は 0.23.2 と完全に同一だった。`think:false` は正常、`think:true` は非thinkingモデルで引き続き `does not support thinking` の400。**この修正は新しいエンジンでも必要**であり、バージョン差の問題ではなく設計上の問題だったことが確定した。`/api/show` の `capabilities` も同じ形で返る。

残課題も解消: カタログ全15タグが公式レジストリに実在することを確認済み（下記）。`ollama_version()` の下限バージョン検証も実装・配線済み。

### (c) 生成のキャンセルが存在しない

`reqwest::Client` にタイムアウト未設定、UIに中断ボタンもない。macOSでは数秒で終わっていた生成が本機では数分になるため、**キャンセル機能は実質必須**。

### その他

- `src/App.tsx:324` の「ターミナルで `ollama serve` を実行してください」はmacOS流儀の案内。Windows版Ollamaはトレイ常駐なのでOS別の文言にする。
- 圧縮後は `[{role:user, content:compressed}]` のみを送るため（`src/App.tsx:304`）、オンライン側の会話文脈が毎ターン切れる。CPU機ではルーター往復が重いので、コストと文脈のトレードオフとして再検討の価値がある。
- `route_and_compress` は履歴を `onlineSelected[0]` のスレッドから代表で取る（`src/App.tsx:291`）ため、複数オンラインを選ぶと他プロバイダの履歴が無視される。

## 5. 実装済みの変更

検証で確定した内容のうち、判断待ちでないものを反映済み。`cargo check` / `cargo test` / `tsc` すべて通過。

| 対象 | 変更 | 根拠 |
|---|---|---|
| `engines/ollama.rs` | `ChatRequest.think` を `Option<bool>` + `skip_serializing_if` に。`capabilities()` / `supports_thinking()` を追加し、`chat_stream` で非対応モデルへの `think` を省いて降格 | 論点(b)・実測 |
| `engines/ollama.rs` | `list_models` が `/api/show` を並列に引いて `supports_thinking` を埋める | 同上 |
| `engines/mod.rs` ほか | `ModelInfo.supports_thinking` を追加。オンライン側は anthropic のみ `true`（実際に `options.think` を extended thinking として転送しているため） | 同上 |
| `credentials.rs` | 資格情報ストアのラウンドトリップ試験＋プロセス跨ぎ永続化試験を追加 | メモ① |
| `engines/ollama.rs` | thinking降格の回帰テスト2件を追加（実機Ollama必要、`--ignored`） | 論点(b) |
| `App.tsx` | 起動時の既定選択を**全モデル → 最小1モデル**に変更 | 論点(a) |
| `App.tsx` | pullしたモデルを選択に自動追加しない（消えたモデルの選択解除のみ行う） | 論点(a) |
| `App.tsx` | 選択モデルの想定常駐量がRAM予算を超えたら警告を表示 | 論点(a) |
| `App.tsx` | じっくりモード時、thinking非対応の選択モデルを名指しで明示 | 論点(b) |
| `App.tsx` | IMEガードに `compositionend` 直後（100ms）のEnter抑止を追加 | メモ⑥ |
| `App.tsx` / `Settings.tsx` | Ollama未接続の案内文と資格情報ストア名をOS別に切り替え | メモ①⑥・その他 |

### このハードで動かすために加えた制約

「このハードで動作するように」という要求に対し、以下を制約として明示的に入れた。いずれも推測ではなくこのマシンでの実測に紐づいている。

| 制約 | 内容 | 根拠 |
|---|---|---|
| **RAM予算による同時実行制御** | `AppState` がRAM予算をセマフォ許可として保持。各ローカルリクエストがモデルの想定常駐量に比例した許可を取得し、埋まっていれば待つ。親モデル（ルーター）も同じ予算を使う | 実測: 並列自体は速い(4-a)。効くのはRAM |
| **速度下限による推奨フィルタ** | 実測スループット÷アクティブサイズが 5 tok/s 未満のモデルを推奨から外し、速い順に並べる。**ただし絶対に空リストにしない**（最低3件は残す） | 実測 49.6 GB/s |
| **測定前は速度を主張しない** | 生成実績がない間は `est_tokens_per_sec` を `null` にし、速度フィルタを無効化。RAM可否のみで判定 | 合成ベンチが不安定だったため(2章) |
| **キャンセルとタイムアウト** | `request_id` 単位の中断。ストリーミングfutureを drop して接続を切る＝プレフィル中でも即座に止まる。UIに個別⏹と全体中断 | 13GBモデルで3.8 tok/s = 分単位 |
| **履歴の上限 (6往復)** | プレフィルは演算律速なので履歴を無制限に送らない。オンライン側にも同じ上限を適用（有料トークンの抑制と、比較の公平性のため） | 4コアで1000トークン=25〜50秒 |
| **短いプロンプトはルーターを呼ばない** | 履歴が空かつ400文字以下なら親モデルを呼ばず、選択された全プロバイダにそのまま送る。文字数（バイト数ではない）で判定 | 親モデルのプレフィルが20〜40秒 |

### 追加した変更（上記制約の実装）

| 対象 | 変更 |
|---|---|
| `catalog.rs` | 全面書き換え。`HardwareProfile` に論理コア数・GPUオフロード有無・実測スループット。`CatalogEntry` に `active_size_gb` と `est_tokens_per_sec`。`recommend()` は「RAM可否 → 速度下限 → 軽い順 → ベンダーround-robin」。降順ソートのバグ修正。テスト7件 |
| `engines/ollama.rs` | `gpu_offload_detected()`（`/api/ps` の `size_vram`）、`model_size_bytes()`、生成完了時に `eval_count`/`eval_duration` から実測スループットを学習（指数平滑、16トークン未満のサンプルは無視） |
| `commands.rs` | `AppState::new` でRAM予算をセマフォ化。`send_chat` が許可取得＋`tokio::select!` によるキャンセル。`cancel_chat` コマンド追加。`detect_hardware`/`recommend_models` を非同期化。入場制御を `permits_for_size_gb`（純粋な計算）と `plan_reservation`（待機の有無を先に返す）に分割してテスト可能に。テスト6件 |
| `Cargo.toml` | tokio に `time` feature を明示追加（`tokio::time::timeout` を直接使うため。従来は tauri 経由で有効になっていただけで脆かった） |
| `router.rs` | `shortcut_decision()` で短いプロンプトのモデル呼び出しを回避。`RouterDecision.shortcut` でUIに区別を伝える。テスト4件 |
| `engines/mod.rs` | `ChatStreamEvent` に `Queued` と `Cancelled` |
| `App.tsx` | 待機中/中断の状態表示、個別⏹と全体中断ボタン、履歴上限、shortcut時は「親モデルが最適化」と表示しない |
| `Catalog.tsx` | 実測スループット・推定tok/s・MoEのアクティブサイズを表示。未測定時はそう明示 |
| `credentials.rs` | プロセス跨ぎ永続化テストを、子プロセスを自ら起動する単一テストに変更（2分割だとアルファベット順でreadがwriteより先に走り `--include-ignored` で失敗した） |

テスト: **23件全通過**（`cargo test --lib -- --include-ignored --test-threads=1`、うち7件は稼働中のOllamaを使う実機テスト）。`cargo build` でリンクまで確認済み。

RAM予算のキュー動作についての補足: この端末の予算22.2GBは、テスト用に入れた小さいモデル（270MB / 522MB）をいくら並べても埋まらないため、通常の操作では `Queued` 経路に入らない。そこで**予算を「モデル1つ分ちょうど」に絞ったテスト**を用意し、(1) 1件目は待たずに通る (2) 2件目は待機と判定される (3) 1件目の解放で2件目が実際に通る（5秒のタイムアウト付き）を確認している。オンライン側が予算に引っかからないことも別テストで固定した。実モデルでの発火は未確認だが、経路自体は検証済み。

未着手（判断待ち・下記7章参照）: 逐次実行モードの選択肢、Ctrl+Enter化、`tauri build`。

## 5.5 レビュー指摘への対応

外部レビューで挙がった5件。いずれも実在の不具合として確認し、修正済み。

### ① 親モデルが「どこにも送らない」と判断できなかった（最も影響が大きい）

`parse_decision` が `providers` 空をパース失敗として扱い、呼び出し側の失敗経路は**選択された全オンラインサービスへの無圧縮送信**だった。つまり「今回は送る価値がない」という最も安い判断が、アプリ中で最も高コストな挙動に化けていた。しかもプロンプト自体が「全部選ぶ必要はない」と指示しているので、正しく従ったモデルが罰される構造だった。

修正: 空の `providers` を正当な判断として受け入れる。候補外の名前だけを返した場合（フィルタ後に空）も同様。一方「プロバイダを選んだのに `compressed_prompt` が空」は課金対象の本文が無いので引き続きエラー。プロンプトにも「どれも価値がなければ `[]` にせよ、それが正しい判断だ」と明記した。UIには「今回はどのサービスも呼び出す価値がないと判断しました。送信していません」と表示する。テスト5件追加。

### ② 親モデル経由だと2ターン目以降のオンライン履歴が失われていた

圧縮後プロンプト1件のみを送っていたため、オンラインモデルは自分の前回回答を踏まえた継続会話にならなかった。

修正: ルーターの契約を変更。`compressed_prompt` は**新しい質問の送信文だけ**を表し、過去のやり取りは各ターゲット自身の履歴として別途送る（上限6往復）。プロンプトにも「過去のやり取りは別途送られるので混ぜるな」と明記。履歴圧縮という機能は失われるが、履歴上限で総量は既に抑えられており、コスト削減の主効果はプロバイダ選別のほうにある。

### ③ ルーティング判断の履歴が先頭プロバイダ1つ分だった

`onlineSelected[0]` 固定だったため、全員に同じ圧縮結果を流すと文脈がずれる。

修正: ルーターへは**ユーザー自身の発言のみ**を渡す。ユーザーの発言は全プロバイダで同一なので、判断が選択順に依存しなくなる。各プロバイダ固有の回答は②の経路で本人にだけ届く。

### ④ ダウンロード中断が成功扱いになりえた

Ollamaは完了時に `success` ステータス行を出し、ループはそれを見た時点で `Done` を送って抜ける。ストリーム末尾に到達するのは**接続断・エンジン再起動・中断**のいずれかなのに、そこでも `Done` を送っていた。半分だけ落ちたモデルが導入済みに見える。

修正: 末尾到達時は `Error`（「ダウンロードが完了前に中断されました」）を送る。

### ⑤ 送信ボタンの件数が実際の送信数とずれた

`selectedIds.length` 依存だったが、オンラインモード切替やモデル一覧更新後は解決できないIDが残り、`handleSend` は黙って捨てていた。

修正: 解決済みターゲットを `sendableTargets` として一箇所で導出し、ボタン件数・活性判定・送信処理・RAM警告・じっくり警告のすべてがこれを参照する。

### あわせて対応

親モデルによる最適化が**失敗して全先に送信するフォールバック**は、従来 `console.error` だけで課金が黙って発生していた。UIに「最適化に失敗したためN サービスへそのまま送信しました」と表示するようにした。ユーザーが自分で選んだ先なので送信自体は維持している。

### モデルチップの並び順とEjectの挙動

- **並び順**: 選択中のチップを先頭に寄せる（各グループ内は元の順序を保つ安定ソート）。トグルするとチップが動くのは仕様上避けられないので、連続クリック時は位置がずれる点に注意
- **Eject後**: Unload成功後にチップを非表示にし、選択も外す。「もう使わない」という意思表示なのでチップが残っていると数GBの再ロードを誘発する
  - 重要: `models`（インストール済み一覧）からは**消さない**。消すとカタログが既にディスクにあるモデルへ「ダウンロード」を出し、設定画面の親モデル候補からも消える。別途 `ejectedModels` として保持し、モデル一覧の明示的な更新で復帰させる
  - Unloadが失敗した場合はチップを消さない（メモリに残っているのに消えると状態を誤報する）

## 5.6 アプリ内からのOllama更新機能

「Ollamaのバージョンアップをアプリから行いたい」という要望への実装。

### 設計上の2つの制約

**インストーラを自分でダウンロードして実行しない。** 更新はOSのパッケージマネージャ（Windows: winget / macOS: Homebrew）に委譲する。パッケージマネージャは自身の署名済みマニフェストで配布物を解決しハッシュを検証するため、我々がインターネットからバイナリを取得して exec するより安全。後者は「何を実行して安全か」の判断をこのアプリが負うことになり、その判断材料を持っていない。パッケージマネージャが無い環境では公式ダウンロードページを開くところまでに留める。

**明示的なクリックなしには何も起きない。** 更新はエンジンを再起動するので、読み込み済みモデルは解放され実行中の生成は死ぬ。自動更新はしない。

### 実装

| 対象 | 内容 |
|---|---|
| `updater.rs`（新規） | 最新版取得（GitHub Releases API）、パッケージマネージャ検出、更新実行と出力ストリーミング |
| `commands.rs` | `EngineVersion` に `latest` / `update_available` / `package_manager` / `download_page` を追加。`update_ollama` コマンド |
| `EngineUpdate.tsx`（新規） | バージョン表示、更新ボタン、更新ログのライブ表示、完了後のリフレッシュ |
| `App.tsx` | 更新があるときメイン画面にバナー表示（⚙️へ誘導） |
| `capabilities/default.json` | `opener:allow-open-url` を明示追加 |

### 設計判断のメモ

- **最新版の取得元は GitHub Releases**。`winget list` は利用可能バージョンも返すが、**出力テーブルがロケール依存**（この端末は日本語の列見出しを出す）でパースが壊れやすい。表示ロジックをwingetの出力形式から切り離し、wingetは「実行」だけに使う。exit code のみ見る
  - 実機で両者が一致することを確認: GitHub `0.32.4` / winget available `0.32.4`
  - wingetのマニフェスト反映が遅れると最新版より少し古いものが入る可能性があるため、UIに注記
- **winget は非対話で起動**（`--disable-interactivity --accept-source-agreements --accept-package-agreements --exact`）。コンソールを持たないプロセスでプロンプトが出ると永久にハングするため。テストで固定
- **`--exact`** を付けて、IDが `Ollama.Ollama` で始まる別パッケージが拾われないようにする
- **出力は `\r` でも分割する**。wingetはダウンロード進捗をキャリッジリターンで再描画するので、`\n` を待つと数百MBのダウンロード中に何も表示されず最後にまとめて出る。連続する同一行は捨てる
- **Windows では `CREATE_NO_WINDOW`** を付けて子プロセスのコンソールが一瞬前面に出るのを防ぐ
- **実行中の生成があるときは更新を拒否**。判定にはRAM予算セマフォの空き許可数を使う（状態を二重に持たない）
- 最新版が取得できなくても（オフライン等）**インストール済みバージョンの表示は失敗させない**
- 更新完了後はバージョンとモデル一覧を再取得する（エンジンが再起動するのでどちらも古い）

### 検証済み

- GitHub Releases API から `0.32.4` を取得
- winget の検出（`Some(Winget)`）
- 非対話引数、タグの `v` 除去、実行中判定
- **実際の更新を実行し成功**（下記）

### 実更新の記録（0.23.2 → 0.32.4）

アプリが実行するのと同一の引数で winget を実走させた。

```
winget upgrade --id Ollama.Ollama --exact --accept-source-agreements --accept-package-agreements --disable-interactivity
```

出力の要点:

```
見つかりました Ollama [Ollama.Ollama] バージョン 0.32.4
ダウンロード中 https://github.com/ollama/ollama/releases/download/v0.32.4/OllamaSetup.exe
インストーラーハッシュが正常に検証されました
インストールが完了しました
```

**「インストーラーハッシュが正常に検証されました」が、パッケージマネージャ経由にした設計判断の実利**そのもの。自前でダウンロードしていればこの検証は存在しない。

確認できたこと:

- 更新後 `ollama --version` → **0.32.4**、`/api/version` も一致
- Ollama本体とトレイアプリが**自動起動**した（手動起動は不要）
- **ローカルモデルは保持された**（`smollm2:135m` / `qwen3:0.6b` とも健在）
- API挙動は 0.23.2 と同一 — `think` も `capabilities` も変化なし
- **全33テストが新バージョンに対して通過**
- ダウンロード進捗が実際にキャリッジリターンで再描画されることを確認。`\r` でも行分割する実装判断が正しかった（`\n` だけを待つと数百MBの間ずっと無反応になる）

事前に実行中の `ollama serve` を停止してから実施した。停止しないとインストーラが使用中のファイルを置き換えられない可能性がある。**アプリ内から更新する場合、Ollamaはトレイアプリとして常駐しているのが通常で、インストーラ側が停止処理を持つ**が、ここは実機での追試が望ましい。

### カタログタグの実在確認

更新の主目的だった懸念（カタログの新しいタグが解決するか）を、公式レジストリのマニフェスト参照で確認した（ダウンロードなし）。

**全15タグが HTTP 200 で実在**: `qwen3.5:9b` / `qwen3.6:35b-a3b` / `qwen3-coder:30b` / `gemma3:4b` / `gemma3:12b` / `gemma3:27b` / `mistral-small:22b` / `mistral-nemo` / `llama3.1:8b` / `llama3.2:3b` / `llama3.3:70b` / `deepseek-r1:8b` / `deepseek-r1:32b` / `phi4:14b` / `gpt-oss:20b`

カタログに存在しないタグは無く、pull失敗の懸念は解消。

## 6. 実装計画

### Phase 0: 環境構築（半日）

- ~~rustup 導入~~ → **導入済み**（cargo/rustc 1.97.1, MSVC host）。`~/.cargo/bin` を User PATH に追加するだけでよい
- ✅ `npm install` 完了（73パッケージ、脆弱性0）
- ✅ `npm run build` 通過（tsc + vite、TypeScript側もクリーン）
- Ollama を最新へ更新し起動
- 軽量モデルを pull: `llama3.2:3b` / `gemma3:4b` / `llama3.1:8b`（まずこの3つで足りる）
- `cargo check` → `npm run tauri dev` でビルド通過を確認（初回 cargo build は10〜20分想定）

### Phase 1: Windows動作検証（メモ7項目の消化）

- ✅ keyring の保存・削除・プロセス跨ぎ永続化（①参照）
- ✅ `think` パラメータの挙動 → バグを発見し修正（論点(b)参照）
- ⏳ IME 変換確定Enterの誤送信 → **GUI操作なので人の手が必要**。ガードは実装済み、実機で「変換候補を出してEnterで確定」を試すこと
- ✅ `ollama_version()` → 下限バージョン検証を実装し配線（従来フロントから呼ばれていなかった）
- ✅ モデル一覧 / unload → 実機で動作確認済み
- ✅ `npm run tauri build`（nsis）→ インストーラ生成成功（④参照）

### Phase 2: CPU-only対応の設計変更（本番）

フロントエンド側と thinking 判定は5章のとおり実装済み。残りは以下。

5章および以下のとおり実装完了。

- ✅ **逐次実行モード**を設定画面のオプションとして追加。既存のRAM予算セマフォを再利用し、有効時は各リクエストが予算全体を確保することで直列化する（第二の機構を足していない）。既定は無効 — 実測では2モデル同時のほうが全体は早く終わるため。「最初の回答を最速で読みたい」という、数値に表れない要求のための選択肢
- ✅ **親モデルの速度警告**。routerロールへの固定はしなかった。カタログのタグをハードコードしても未インストールなら意味がなく、実測値で判断できるほうが汎用的。推定速度が 10 tok/s を下回る親モデルを選ぶと、設定画面に推定値つきの警告を出す
- ✅ **`connect_timeout` を5秒に設定**（全エンジン共通の `engines::http_client()`）。ストリーミングに全体timeoutは付けない — GPUなしでは長い回答が分単位になるのが正常で、期限を切ると健全な生成を打ち切ってしまう。一方で死んだエンジンや到達不能なAPIは数秒で失敗すべき
- ✅ **Ollamaの下限バージョン検証**。`ollama_version` コマンドは登録済みだったがフロントから呼ばれておらず死んでいた。下限は **0.9.0**（`think` と `/api/show` の `capabilities` がどちらも0.9で入ったため）。それ以上を要求はしない — 新しいモデルタグの解決にはより新しいエンジンが必要だが、それはレジストリ側の問題でこの検証では答えられず、動いているエンジンを拒否するほうが害が大きい。パース不能なバージョンは「対応」扱いにする（認識できない形式は古いエンジンより新しいエンジンである可能性が高く、毎回の誤警告のほうが有害）

### Phase 3: 配布

- ✅ `bundle.targets` を `"all"` → `["nsis"]` に変更
  - MSIはWiX 3依存で、非ASCIIパスや制限された端末で失敗しやすい。社内配布に2つの形式を持つ利点よりコストが大きい
  - 注意: `tauri.conf.json` はスキーマが未知フィールドを拒否するため、**JSONにコメントを書けない**（`"// targets"` を入れると `unknown field` でビルド失敗する）。設定の意図はこのドキュメント側に置く
- 未署名配布時のSmartScreenについて（下記）

#### 未署名バイナリとSmartScreen

コード署名証明書を入れない限り、初回起動時に「WindowsによってPCが保護されました」が出る。これは想定内の挙動で、アプリの不具合ではない。社内配布時の案内文として:

1. 警告画面で「詳細情報」をクリック
2. 「実行」を選択

ダウンロード経由で配布すると Mark-of-the-Web が付くため警告は確実に出る。社内ファイルサーバーからのコピーやUNCパス経由なら出ないこともあるが、環境依存なので前者を前提に案内するのが安全。

恒久的に消すには EV コード署名証明書（年額数万円〜）が必要。社内限定なら上記案内で足りるかは要判断（7章）。

### Phase 4（任意）: iGPU活用

生成速度はiGPUでも帯域が同じなので改善しないが、**プレフィルは演算律速なので Iris Xe で2〜4倍改善の可能性**がある。ただし公式Ollamaでは不可で、ipex-llm版など別バックエンドの追加になるため費用対効果は要判断。

## 7. 残っている判断・作業

コードで閉じられる範囲は完了。残りは指示か人手が必要なものだけ。

### 導入済みモデル

`llama3.2:3b`（2.0GB）/ `gemma3:4b`（3.3GB）/ `llama3.1:8b`（4.9GB）を導入し、速度モデルの検証に使用済み（2章）。カタログ記載サイズと実サイズは完全一致した。検証用に `smollm2:135m`（270MB、非thinking）と `qwen3:0.6b`（522MB、thinking対応）も保持。後者2つは capability ゲートの両側をテストするために必要。

### 人手が必要

- **IMEの変換確定Enter** — GUI操作なので実機確認が必要。ガードは実装済み。`target/release/bundle/nsis/LocalAgentsOps_0.1.0_x64-setup.exe` を入れて「変換候補を出してEnterで確定」を試すこと
- 実モデルでのRAM予算キュー発火（小さいモデルでは予算が埋まらない。経路自体は単体テスト済み）

### 判断待ち

- **Enter送信を Ctrl+Enter に変更するか** — 見送り中。preference であって不具合ではなく、Enter送信を奪うと非IMEユーザーには機能後退になる。バグ自体は100msガードで対処済み
- **モデルチップの並び替えを即時にするか遅延させるか** — 現在は即時（選択中を先頭）。トグルするとチップが動くので、連続クリック時に対象がずれる
- **未署名配布（SmartScreen警告）を許容するか** — Phase 3 の案内文で足りるか
- **Phase 4 のiGPU対応に踏み込むか** — 公式Ollamaでは不可、ipex-llm等の別バックエンド追加になる

### 決定済み

- ~~並列比較をCPU機では逐次実行に落とすか~~ → RAM予算制御を既定とし、逐次実行は設定画面のオプションとして提供
- ~~MSI を捨ててNSISのみにするか~~ → NSISのみ（④）
- ~~親モデルを routerロールに固定するか~~ → 固定せず、遅い親モデルを選んだときに警告
- ~~`~/.cargo/bin` を User PATH に追加するか~~ → 追加せず作業完了。必要なら下記1行で足りる
  ```
  [Environment]::SetEnvironmentVariable("Path", [Environment]::GetEnvironmentVariable("Path","User") + ";$env:USERPROFILE\.cargo\bin", "User")
  ```
